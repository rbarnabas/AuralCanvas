# Optional app icons for production installers.
#
# Add these files before release builds for branded installers:
#   icon.icns  — macOS (1024×1024 source recommended)
#   icon.ico   — Windows (256×256 multi-size .ico)
#   icon.png   — Linux / fallback (512×512 PNG)
#
# electron-builder uses the default Electron icon when these are absent.
